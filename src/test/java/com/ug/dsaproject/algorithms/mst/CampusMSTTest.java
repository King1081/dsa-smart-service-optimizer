package com.ug.dsaproject.algorithms.mst;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

/**
 * OWNERS: Papah Kweku Okae Quansah (you) & Abel Asare Boateng
 * Aim for 3-4 tests each toward the group's 40+ total (6-8 combined).
 */
class CampusMSTTest {

    // TODO: runKruskal() on a small hand-built graph produces (V-1) edges
    // TODO: runPrim() on the same small graph produces the same total weight as Kruskal
    // TODO: MST total weight matches a hand-calculated expected value
}
